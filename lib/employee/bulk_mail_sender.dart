import 'package:flutter/material.dart';

class BulkMailSender extends StatelessWidget {
  const BulkMailSender({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}